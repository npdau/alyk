name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm check

      - name: Build
        run: pnpm build

      # Optional: Upload build artifacts
      # - name: Upload build
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: dist
      #     path: dist/

  # Optional: Deploy to preview on PRs
  # deploy-preview:
  #   runs-on: ubuntu-latest
  #   needs: lint-and-build
  #   if: github.event_name == 'pull_request'
  #   steps:
  #     - name: Deploy to preview
  #       run: echo "Add your preview deployment step here"

  # Optional: Deploy to production on main
  # deploy-production:
  #   runs-on: ubuntu-latest
  #   needs: lint-and-build
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   steps:
  #     - name: Deploy to production
  #       run: echo "Add your production deployment step here"
